spring:
  application:
    name: accelerator-api
  datasource:
    url: \
    username: \
    password: \
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: accelerator-api-pool
      maximum-pool-size: \
      minimum-idle: \
      connection-timeout: \
      validation-timeout: \
      initialization-fail-timeout: 1

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

aira:
  service:
    name: accelerator-api
    host-port: 9090
    persistence:
      required: true
      fail-closed: true