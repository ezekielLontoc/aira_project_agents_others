spring:
  application:
    name: accelerator-security
  datasource:
    url: \
    username: \
    password: \
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: accelerator-security-pool
      maximum-pool-size: \
      minimum-idle: \
      connection-timeout: \
      validation-timeout: \
      initialization-fail-timeout: 1

server:
  port: 9091

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

aira:
  service:
    name: accelerator-security
    host-port: 9091
    persistence:
      required: true
      fail-closed: true